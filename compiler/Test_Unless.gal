statement Unless unless If
{
    argument Condition;
    fallback 'if (not ' [my Condition Fallback] ')' (i Gal_Block);
}

unless (string.eq (firstchar X) 'Z')
{
    writeline "No it's not a Z word.";
}
